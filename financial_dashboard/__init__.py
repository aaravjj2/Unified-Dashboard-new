
"""financial_dashboard package initializer.

Keep this file minimal so the package can be imported when tab
modules are executed dynamically. Avoid non-Python content which
will break importlib execution when the package is loaded via spec.
"""

__all__ = ["tabs", "utils"]

__version__ = "0.0.1"
