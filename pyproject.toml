[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "unified-dashboard"
version = "0.0.1"
description = "Unified Financial Dashboard - local editable install"
readme = "README.md"
authors = [ { name = "Dev" } ]
requires-python = ">=3.10"
dependencies = []
