name: Deep-Tech E2E

on:
  push:
    branches: [ main, master ]
  pull_request:

jobs:
  e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r alpaca-options-lab/requirements.txt || true
          pip install pytest playwright
          playwright install --with-deps || true
      - name: Start app (background)
        run: |
          cd alpaca-options-lab
          nohup python run.py &
          sleep 5
      - name: Run E2E tests
        run: |
          cd alpaca-options-lab
          timeout 120 python tests/test_deeptech_e2e.py
